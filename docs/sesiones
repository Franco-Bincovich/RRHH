# Sesiones de trabajo — HR Karstec

> Derivado de `docs/PLAN-DE-TRABAJO.md` (5/8/2026). No lo reemplaza: lo convierte en una lista
> ejecutable. Cada fila de acá es **una sesión** — un objetivo, un commit.
>
> 🖥️ = sesión de Claude Code · 👤 = acción de Franco fuera del código · 🔀 = las dos
> Dificultad: **1** trivial · **2** acotada · **3** requiere diseño · **4** toca muchos archivos
> Esfuerzo: **S** ≤1 h · **M** 1-3 h · **L** >3 h

**Criterio de orden.** Primero lo que destraba a otros, después lo que rompe, después lo nuevo,
al final la limpieza. Dentro de cada bloque, de menor a mayor dificultad.

**Regla que gobierna todo:** lo que aparezca en el camino y no esté acá se anota en
`DEUDA-TECNICA.md` y se sigue.

---

## Bloque 0 — Higiene y seguridad (antes que nada)

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S1** | 🔴 **Rotar el `GOOGLE_CLIENT_SECRET`** en la consola de Google y actualizarlo en Vercel. No invalida los refresh tokens ya emitidos | 👤 | 1 | S | F0.1 |
| **S2** | Rescatar el commit huérfano `aa15022` (`git branch rescate/aa15022 aa15022`). Vence ~22/8 por `git gc` | 👤 | 1 | S | — |
| **S3** | Revisar la consola de Google: Gmail API habilitada · redirect URI exacta · los 4 scopes · consent screen en *Testing* o *In production* | 👤 | 2 | S | F0.1 |
| **S4** | Confirmar las 4 variables de Google en Vercel `sofia-backend` + redeploy + `curl /health` | 👤 | 1 | S | F0.2 |

---

## Bloque 1 — Verificar lo que ya está construido y nunca corrió

Son minutos cada una y ejercitan semanas de código escrito a ciegas. **No dependen de nada.**

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S5** | Abrir `NominaModal` y `VacanteModal` en pantalla chica: encabezado y pie fijos, scroll en el medio | 👤 | 1 | S | V8 |
| **S6** | Sesión de 8 h y baja blanda: dar de baja un usuario de prueba → 403 en ≤60 s, y revertir con las **dos** mitades | 🔀 | 2 | S | V7 |
| **S7** | Cargar 1 vacación a mano y confirmar que la pantalla y el reporte R11 dan **el mismo número** | 🔀 | 2 | S | V5 |
| **S8** | Crear un usuario `mandos_medios` y verificar que ve a sus subordinados de **las dos empresas**, y nada más | 🔀 | 3 | M | V6 |

---

## Bloque 2 — Destrabar Google y mails

**El módulo de mails entero es inalcanzable hoy.** `set_remitente()` existe y nadie lo llama.

| # | Sesión | Quién | Dif. | Esf. | Depende de |
|---|---|---|---|---|---|
| **S9** | Crear **1 vacante** con los cinco campos de requisitos poblados. Sirve para S10, para V4 y para la Fase 2 | 👤 | 1 | S | — |
| **S10** | Disparar una lectura de Gmail y confirmar que `updated_at` avanza y `token_expiry` queda en el futuro. **Si no avanza, el refresh está roto y se para acá** | 🔀 | 2 | S | S3, S9 |
| **S11** | 🔴 **Endpoint + toggle para designar la casilla del sistema.** `POST /api/integraciones/google/remitente`, gate INTEGRACIONES+WRITE, toggle en `IntegracionesSection.tsx`. Rechazar cuentas sin `gmail.send`. Tests: que marcar desmarque la anterior, y que una cuenta sin `gmail.send` no se pueda marcar | 🖥️ | 3 | M | S10 |
| **S12** | Crear la primera plantilla de mail desde `/configuracion` y previsualizarla contra un empleado real, sin variables sin resolver | 👤 | 1 | S | S11 |
| **S13** | **Enviar el primer mail** a la propia casilla. Verificar las tres cosas: llega · 1 fila en `mail_enviado` con `estado='enviado'` · 1 evento en `auditoria` | 🔀 | 2 | S | S12 |
| **S14** | **Probar el camino de fallo**: mandar a una dirección inválida. Tiene que quedar 1 fila `estado='fallido'` con el motivo **y después** propagar el error | 🔀 | 2 | S | S13 |
| **S15** | Tests de `plantillas_service.guardar/borrar` y de `mail_enviado_repo` | 🖥️ | 2 | M | S13 |
| **S16** | Guarda de baja (409) del usuario que sostiene la casilla del sistema | 🖥️ | 2 | S | S11 |

---

## Bloque 3 — Fixes de comportamiento (antes de que la segunda empresa opere)

Hoy hay 2 empresas cargadas pero una sola opera. Cada evento mal etiquetado es un dato perdido en
una tabla inmutable.

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S17** | Auditar el import de costos: `batch_upsert_nomina` no emite un solo evento | 🖥️ | 2 | S | E1 |
| **S18** | `_costos_write.py:80` audita con la empresa del **header**, no la de la entidad. Junto: los 3 eventos ya mal etiquetados | 🖥️ | 2 | S | E2 |
| **S19** | Barrido estructural: "todo repo con método de escritura tiene su evento de auditoría". Arreglar la instancia no cierra la clase | 🖥️ | 3 | M | T11 |
| **S20** | Barrido estructural: "todo `_*.py` público tiene al menos un caller". Habría detectado `set_remitente` con cero callers | 🖥️ | 3 | M | T12 |
| **S21** | Migrar `objetivos.responsable_id` de FK a `users` → FK a `empleados`. **Hay 1 sola fila: hoy es trivial, con datos es cara** | 🖥️ | 3 | M | E4 |

---

## Bloque 4 — CV screening, fase 1 de 3

Solo bajar el adjunto y crear el candidato. El matcher y el clasificador son fases aparte.

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S22** | Mandar a la casilla un mail con un PDF adjunto desde otra cuenta. **Primero, no último**: sin un mail real no se conoce la forma del árbol MIME | 👤 | 1 | S | F2.0 |
| **S23** | Decidir `D5`: ¿un candidato por mail o por adjunto? Cambia la constraint de la migración 093 | 👤 | 2 | S | D5 |
| **S24** | Dividir `routers/vacantes.py` (80/80) — los 3 endpoints de candidatos/gmail a un router propio | 🖥️ | 2 | S | F2.1 |
| **S25** | Dividir `services/vacante_service.py` (149/150) → `_vacante_codigo.py` | 🖥️ | 2 | S | F2.1 |
| **S26** | Dividir `services/gmail_service.py` → `_gmail_adjuntos.py` (recorrido MIME + descarga + decode) | 🖥️ | 2 | S | F2.1 |
| **S27** | Mover la lectura de Gmail a la casilla del sistema (`get_remitente()` en vez de la del usuario) | 🖥️ | 2 | S | F2.2 |
| **S28** | Migración **093**: `candidatos.gmail_message_id` + UNIQUE. Cierra un bug vivo: hoy llamar dos veces crea dos candidatos | 🖥️ | 2 | S | F2.4 |
| **S29** | **Bajar el adjunto**: `format=full` · recorrido recursivo de `parts[]` · llamada a `/attachments/{id}` · decode base64url con padding | 🖥️ | 3 | M | F2.3 |
| **S30** | Mails sin match visibles: candidato con `vacante_id = NULL` + filtro en el listado **y en el export** | 🖥️ | 2 | S | F2.5 |
| **S31** | Botón `POST /api/vacantes/revisar-casilla` + extraer el presupuesto de tiempo genérico de `_lote_mails.py` | 🖥️ | 3 | M | F2.6 |
| **S32** | Tests del módulo: árbol MIME real anidado · decode con padding faltante · idempotencia | 🖥️ | 3 | M | F2.7 |

---

## Bloque 5 — Limpieza (no bloquea nada)

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S33** | Purgar los 2 `oauth_states` colgados. Se limpian solos en la próxima conexión | 👤 | 1 | S | E8 |
| **S34** | 🟠 **Poner `CLAUDE.md`, `DEPLOY.md`, `BITACORA-CAMBIOS.md` y `DEUDA-TECNICA.md` al día.** Los cuatro mienten hoy | 🖥️ | 2 | M | E7 |
| **S35** | Los 2 hooks del front sobre 80 líneas | 🖥️ | 2 | S | E5 |
| **S36** | Dividir `objetivos.py` e `inventario_items.py` + `shared_limit` de export | 🖥️ | 2 | S | E6 |
| **S37** | Test que compare `aplicar_filtro_estado` con `derive_estado` | 🖥️ | 2 | S | T13 |
| **S38** | Los 35 archivos del front sobre 150 líneas. `costos/page.tsx` 624 y `vacantes/[id]/page.tsx` 577 a la cabeza. **Varias sesiones, una por archivo** | 🖥️ | 4 | L | E9 |
| **S39** | Las 7 comparaciones `empresa_id !=` post-lectura. **No hay oráculo**: es elegancia, no seguridad | 🖥️ | 4 | L | E10 |

---

## Bloque 6 — AWS (a cargo del otro dev, con decisiones tuyas)

| # | Sesión | Quién | Dif. | Esf. | Origen |
|---|---|---|---|---|---|
| **S40** | 🔴 **Decidir Storage: Supabase o S3.** Antes de la Entrega 2 de evaluaciones, o se hace dos veces. Hoy hay 2 objetos: nunca va a ser más barato | 👤 | 2 | S | A10 |
| **S41** | Dropear la FK `users.id → auth.users(id)` + `DEFAULT gen_random_uuid()`. Sin esto no se puede insertar un usuario del otro lado | 🖥️ | 3 | M | A8 |
| **S42** | Recrear los triggers `updated_at`. En producción hay **50** (39 + 11 `trg_emp_*`), no 36 ni 45 | 🖥️ | 3 | M | A6 |
| **S43** | Parametrizar `_BUCKET` + E2E real de adjuntos, que nunca corrió | 🖥️ | 3 | M | A7 |
| **S44** | Recalibrar `TRUSTED_PROXY_HOPS` y re-medir los 4 techos contra el ALB | 🔀 | 3 | M | A3, A4 |
| **S45** | Porteo de los 69 repos a asyncpg. **Muchas sesiones, una por repo** | 🖥️ | 4 | L | A1 |
| **S46** | Limpiar las 6 tablas huérfanas y las `ev_*` | 🖥️ | 2 | S | A9 |

---

## No son sesiones — esperan a otros

**RRHH (10):** archivo real de vacaciones/ausencias con DNI o legajo · cargar `costos_nomina` ·
deduplicar `GESTION DE DEUDA` · definir si `valor_hora = 0` es cero o "no sabemos" · segundo lote
de evaluaciones · `seniority` · el superior pendiente · sus tipos de ausencia propios · qué manda
en el asunto de un mail de postulación · exportar DNI en evaluaciones.

**Definiciones tuyas (9):** link público de carga de horas (reunión) · ventana temporal del filtro
por proyecto · plantillas de mail base · borrado de plantillas en la UI · extractor de PDF ·
plantillas de onboarding públicas/privadas · qué es un "equipo" · `adjuntos` de evaluación.

---

## Resumen

| Bloque | Sesiones | Qué destraba |
|---|---|---|
| 0 — Higiene | S1–S4 | Seguridad y prerrequisitos |
| 1 — Verificar | S5–S8 | Ejercita semanas de código nunca corrido |
| 2 — Google y mails | S9–S16 | **El módulo de mails entero** |
| 3 — Fixes | S17–S21 | Datos que se pierden al operar la 2ª empresa |
| 4 — CV screening | S22–S32 | La feature nueva |
| 5 — Limpieza | S33–S39 | Nada, pero baja el costo de todo lo demás |
| 6 — AWS | S40–S46 | El cutover |

**Si hay que elegir una sola:** `S11`. Es el único ítem del que cuelga un módulo completo ya
construido.