# HR Karstec — Backend dependencies
# Versiones exactas para evitar CVEs silenciosos (ver SEGURIDAD-PENTEST.md)
# Actualizar mensualmente: pip-audit --fail-on-vuln

PyJWT[crypto]==2.10.1
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
supabase==2.9.1
anthropic==0.34.2
slowapi==0.1.9
resend==2.3.0
# Extracción de texto de CVs en PDF (fase 2 del CV screening).
# 🔴 PURO PYTHON (wheel py3-none-any, 297 KB / 1,16 MB descomprimido) y CERO dependencias
# obligatorias en 3.11+: el único `Requires-Dist` sin extra es `typing_extensions`, y solo para
# python<3.11. Sin binarios nativos, así que `@vercel/python` no tiene nada que compilar.
# Licencia BSD. PyMuPDF quedó descartado por AGPL: es un producto comercial.
# ⚠️ Los PDF cifrados con AES necesitan `cryptography`, que YA viene por PyJWT[crypto] y
# python-jose[cryptography]. No se agrega el extra `pypdf[crypto]` para no duplicar el pin.
pypdf==5.1.0
openpyxl==3.1.5
reportlab==4.2.5
python-docx==1.1.2
python-multipart==0.0.12
httpx==0.27.2
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
